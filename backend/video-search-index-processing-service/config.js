const {
  NODE_ENV,
  KAFKA_BROKER,
  KAFKA_TOPIC,
  ELASTICSEARCH_NODE,
  ELASTICSEARCH_INDEX,
} = process.env;

export {
  NODE_ENV,
  KAFKA_BROKER,
  KAFKA_TOPIC,
  ELASTICSEARCH_NODE,
  ELASTICSEARCH_INDEX,
};
